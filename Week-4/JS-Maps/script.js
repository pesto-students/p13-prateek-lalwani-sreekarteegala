let sentence = prompt("Enter the text to Calculate Word Frequency");

function calcWordFrequencies(word){
    let allWords = word.split(' ');

    let filWords = [...new Set(allWords)]

    let opt = allWords.map(e => )
}

calcWordFrequencies(sentence)